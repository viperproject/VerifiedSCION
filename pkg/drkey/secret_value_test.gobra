package drkey

import "encoding/hex"

func TestDeriveSV() {

	asSecret := []byte{0, 1, 2, 3, 4, 5, 6, 7, 0, 1, 2, 3, 4, 5, 6, 7}

	got, err := DeriveSV(0, NewEpoch(0, 1), asSecret)
    assert err == nil

	goldenRaw := []byte(keyStr)
	var expectedKey Key

	goldenKey, err := hex.DecodeString(string(goldenRaw))

	assert err == nil

	copy(expectedKey[:], goldenKey)

	assert expectedKey == got.Key
}
